/**
 * Video Time is a concrete strategy that will manage the page time
 */
class VideoTime implements IStrategy {
    /**
     * 
     * @param timeObj - as the time object literal.
     */
    doAction(timerObj:any):any {
        
    }
}

export default VideoTime;